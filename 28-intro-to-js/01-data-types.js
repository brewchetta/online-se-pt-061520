// String
"I am a string"
`I am a string with interpolation: ${1 + 1}`

// Integer / Float
1 === 1.0

// Boolean
true
false

// Hash, wait no it's an object
const newObj = {
  name: "An object",
  nestedNames: {
    name: "a nested object"
  }
}

newObj.nestedNames.name

// Array
[1,2,3]
